# Generated lock file - do not hand-edit. Source of truth is base.in
# (direct dependencies); this file is base.in's fully resolved closure,
# every package exactly pinned, verified installable together.
#
# pandas / python-dateutil / six were removed here: pandas was only used by
# legacy_search/service.py's CSV lookup, now replaced with the stdlib csv
# module (src/legacy_search/service.py), and python-dateutil/six were only
# ever pulled in transitively by pandas - nothing in src/ imports any of
# the three any more (verified by grep, not assumed).
annotated-doc==0.0.5
annotated-types==0.8.0
anyio==4.14.2
certifi==2026.7.22
click==8.4.2
fastapi==0.141.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
numpy==2.5.1
pydantic==2.13.4
pydantic_core==2.46.4
python-dotenv==1.2.2
starlette==1.3.1
typing-inspection==0.4.2
typing_extensions==4.16.0
uvicorn==0.52.0
