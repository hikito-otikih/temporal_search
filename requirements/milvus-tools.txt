# Optional tier for scripts that talk to Milvus directly (migration/
# diagnostic tooling, e.g. research_tools/ scripts that read a Milvus Lite
# export or query the standalone server) - not imported by src/ itself,
# which only ever reaches Milvus indirectly through the upstream retrieval
# service on port 8000 (a separate project, aic2026/text_retrieval, with its
# own pymilvus dependency). pyarrow is needed alongside pymilvus for reading
# Milvus's parquet segment files directly when its own query engine proves
# unreliable for a given export (see migrate_milvus_lite_to_server.py's
# docstring for a real instance of this).
-r base.txt

pymilvus[milvus_lite]
pyarrow
